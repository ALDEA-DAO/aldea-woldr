// SPDX-License-Identifier: MIT
pragma solidity >=0.8.24;

/* Autogenerated file. Do not edit manually. */

// Import store internals
import { IStore } from "@latticexyz/store/src/IStore.sol";
import { StoreSwitch } from "@latticexyz/store/src/StoreSwitch.sol";
import { StoreCore } from "@latticexyz/store/src/StoreCore.sol";
import { Bytes } from "@latticexyz/store/src/Bytes.sol";
import { Memory } from "@latticexyz/store/src/Memory.sol";
import { SliceLib } from "@latticexyz/store/src/Slice.sol";
import { EncodeArray } from "@latticexyz/store/src/tightcoder/EncodeArray.sol";
import { FieldLayout } from "@latticexyz/store/src/FieldLayout.sol";
import { Schema } from "@latticexyz/store/src/Schema.sol";
import { EncodedLengths, EncodedLengthsLib } from "@latticexyz/store/src/EncodedLengths.sol";
import { ResourceId } from "@latticexyz/store/src/ResourceId.sol";

library TribeBonus {
  // Hex below is the result of `WorldResourceIdLib.encode({ namespace: "aldea", name: "TribeBonus", typeId: RESOURCE_TABLE });`
  ResourceId constant _tableId = ResourceId.wrap(0x7462616c6465610000000000000000005472696265426f6e7573000000000000);

  FieldLayout constant _fieldLayout =
    FieldLayout.wrap(0x0004010004000000000000000000000000000000000000000000000000000000);

  // Hex-encoded key schema of (uint32, uint32)
  Schema constant _keySchema = Schema.wrap(0x0008020003030000000000000000000000000000000000000000000000000000);
  // Hex-encoded value schema of (uint32)
  Schema constant _valueSchema = Schema.wrap(0x0004010003000000000000000000000000000000000000000000000000000000);

  /**
   * @notice Get the table's key field names.
   * @return keyNames An array of strings with the names of key fields.
   */
  function getKeyNames() internal pure returns (string[] memory keyNames) {
    keyNames = new string[](2);
    keyNames[0] = "tribe";
    keyNames[1] = "resourceType";
  }

  /**
   * @notice Get the table's value field names.
   * @return fieldNames An array of strings with the names of value fields.
   */
  function getFieldNames() internal pure returns (string[] memory fieldNames) {
    fieldNames = new string[](1);
    fieldNames[0] = "bonusPercentage";
  }

  /**
   * @notice Register the table with its config.
   */
  function register() internal {
    StoreSwitch.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Register the table with its config.
   */
  function _register() internal {
    StoreCore.registerTable(_tableId, _fieldLayout, _keySchema, _valueSchema, getKeyNames(), getFieldNames());
  }

  /**
   * @notice Get bonusPercentage.
   */
  function getBonusPercentage(uint32 tribe, uint32 resourceType) internal view returns (uint32 bonusPercentage) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get bonusPercentage.
   */
  function _getBonusPercentage(uint32 tribe, uint32 resourceType) internal view returns (uint32 bonusPercentage) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get bonusPercentage.
   */
  function get(uint32 tribe, uint32 resourceType) internal view returns (uint32 bonusPercentage) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    bytes32 _blob = StoreSwitch.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Get bonusPercentage.
   */
  function _get(uint32 tribe, uint32 resourceType) internal view returns (uint32 bonusPercentage) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    bytes32 _blob = StoreCore.getStaticField(_tableId, _keyTuple, 0, _fieldLayout);
    return (uint32(bytes4(_blob)));
  }

  /**
   * @notice Set bonusPercentage.
   */
  function setBonusPercentage(uint32 tribe, uint32 resourceType, uint32 bonusPercentage) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((bonusPercentage)), _fieldLayout);
  }

  /**
   * @notice Set bonusPercentage.
   */
  function _setBonusPercentage(uint32 tribe, uint32 resourceType, uint32 bonusPercentage) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((bonusPercentage)), _fieldLayout);
  }

  /**
   * @notice Set bonusPercentage.
   */
  function set(uint32 tribe, uint32 resourceType, uint32 bonusPercentage) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreSwitch.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((bonusPercentage)), _fieldLayout);
  }

  /**
   * @notice Set bonusPercentage.
   */
  function _set(uint32 tribe, uint32 resourceType, uint32 bonusPercentage) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreCore.setStaticField(_tableId, _keyTuple, 0, abi.encodePacked((bonusPercentage)), _fieldLayout);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function deleteRecord(uint32 tribe, uint32 resourceType) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreSwitch.deleteRecord(_tableId, _keyTuple);
  }

  /**
   * @notice Delete all data for given keys.
   */
  function _deleteRecord(uint32 tribe, uint32 resourceType) internal {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    StoreCore.deleteRecord(_tableId, _keyTuple, _fieldLayout);
  }

  /**
   * @notice Tightly pack static (fixed length) data using this table's schema.
   * @return The static data, encoded into a sequence of bytes.
   */
  function encodeStatic(uint32 bonusPercentage) internal pure returns (bytes memory) {
    return abi.encodePacked(bonusPercentage);
  }

  /**
   * @notice Encode all of a record's fields.
   * @return The static (fixed length) data, encoded into a sequence of bytes.
   * @return The lengths of the dynamic fields (packed into a single bytes32 value).
   * @return The dynamic (variable length) data, encoded into a sequence of bytes.
   */
  function encode(uint32 bonusPercentage) internal pure returns (bytes memory, EncodedLengths, bytes memory) {
    bytes memory _staticData = encodeStatic(bonusPercentage);

    EncodedLengths _encodedLengths;
    bytes memory _dynamicData;

    return (_staticData, _encodedLengths, _dynamicData);
  }

  /**
   * @notice Encode keys as a bytes32 array using this table's field layout.
   */
  function encodeKeyTuple(uint32 tribe, uint32 resourceType) internal pure returns (bytes32[] memory) {
    bytes32[] memory _keyTuple = new bytes32[](2);
    _keyTuple[0] = bytes32(uint256(tribe));
    _keyTuple[1] = bytes32(uint256(resourceType));

    return _keyTuple;
  }
}
